{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "//": "Local-only credentials matching infra/docker-compose.yml. NOT a secret. Real environments read from Secrets Manager.",
    "Postgres": "Host=localhost;Port=5432;Database=erp;Username=erp;Password=erp_local_dev"
  },
  "Dev": {
    "//": "Development-only. Bootstraps a demo workspace + owner so the SPA is usable. Never enabled outside Development.",
    "SeedDemoWorkspace": true,
    "DemoSlug": "demo",
    "DemoEmail": "owner@demo.test",
    "DemoPassword": "Demo1234!"
  }
}
